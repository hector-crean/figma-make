/**
 * Script to export bibliography as JSON for use in Figma plugin
 * 
 * Usage: node scripts/export-bibliography.js
 * 
 * Note: This requires the TypeScript files to be compiled first, or use tsx/ts-node
 */

// For now, this is a placeholder. The actual implementation would need:
// 1. TypeScript compilation, or
// 2. tsx/ts-node installed, or  
// 3. Convert references.ts to .js

// Alternative: Create bibliography.json manually or use a build step
console.log('To export bibliography, run: npm run export:bibliography');
console.log('This requires tsx to be installed: npm install -D tsx');

